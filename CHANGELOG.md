# 更新日志 / Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号语义 [SemVer](https://semver.org/lang/zh-CN/)。

发布方式：打 tag（`git tag v0.x.y && git push --tags`）后在 GitHub
Releases 页面新建 Release，标题即版本号，正文从本文件复制对应小节。

## [Unreleased]

## [0.3.0] — 2026-08-23

### 新增
- 搜索会话懒加载：总数即时返回、内容按页（300 条）按需拉取，十万级结果不卡顿
- 搜索语法：大小（`>10mb`、`1mb..50mb`）与日期（`today`、`>2024-01-01`）过滤
- 类型筛选芯片（图片/视频/音频/文档/APK/压缩包，引擎侧过滤）
- 搜索历史与建议（输入框下拉，可单条删除/清空）
- 搜索结果关键词高亮；点结果路径可把范围限定到该文件夹
- 内置图片查看器（缩放/滑动切换/列表缩略图）、视频/音频播放器（ExoPlayer）
- 文本预览：等宽渲染、文内搜索高亮跳转、横向滑动、root 区读取
- 压缩包虚拟层级浏览：zip 与 rar（NDK 编译 unrar），内部文件可打开/预览/校验
- 回收站：删除默认进回收站（可恢复/清空），确认框可选永久删除
- 书签二级页 + 详情面板收藏
- 大文件查重：大小阈值（用户输入）→ 大小分组 → 头 1MB 指纹精筛，可批量清理
- APK 管理：全盘安装包按包名分组，一键选中旧版本清理
- APK 直装（root/Shizuku 静默）、MD5/SHA1/SHA256 校验（进度百分比）
- 设置：紧凑数据库（8KB 页）开关 + VACUUM 压缩（含耗时估算）
- 排序底部面板（含显示隐藏文件开关）；顶栏点标题输入路径跳转

### 修复
- 改词搜索（j→jp）卡骨架屏：旧会话 id 拉页 MISS 留下孤儿占位符
- Android 15 toybox `find -exec +` 不守 ARG_MAX 导致 root 区同步输出残缺（改 xargs 管道）
- watch 限额不足时不再降级：root 下按需提升、停止恢复
- 搜索会话钉扎旧索引快照的内存泄漏（690MB → 378MB 稳态）
- 图片查看器首次解码后无限转圈（frameBuilder 条件错误）
- 多选背景不变色（ListTile 只认 selectedTileColor）
- 增量同步导致界面闪烁（无缝刷新，不清多选）
- 空间分析展开无法收回；饼图返回直接退出页面
- 压缩包内搜索退化为全盘（改为本地过滤）
- 文本预览无法左右滑动（共用横向控制器行间联动）

### 变更
- 侧边栏紧凑化，新增书签/回收站/查重/APK 管理入口
- 校验等只读 IO 与同步队列分离（独立线程，不再排队秒级）

## [0.2.0] — 2026-08-22

### 新增
- 复制/移动（目标选择器 + 多选操作栏）
- WizTree 式空间分析（树形 + 饼图）
- 按应用检索、搜索结果进入文件夹、路径输入跳转
- 详情面板文本可选中复制、块状操作按钮

## [0.1.0] — 2026-08-21

- 首个可用版本：即时搜索、三层特权访问、文件管理、
  SoA 内存索引、DB v3、前台实时监听
